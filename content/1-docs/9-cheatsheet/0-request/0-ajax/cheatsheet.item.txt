Title: $request->ajax()

----

Excerpt: Checks if this request is an Ajax request

----

Return:

type: boolean
text:

----

Text:

```php
if(kirby()->request()->ajax()) {
  echo 'This is an ajax request'
}
```
