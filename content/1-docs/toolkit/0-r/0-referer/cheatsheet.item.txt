Title: r::referer

----

Excerpt: Returns the referer if available

----

Call: r::referer($default = null)

----

Return: type: string
text:

----

Params: - 
  name: $default
  type: string
  text: >
    Pass an optional URL to use as default
    referer if no referer is being found